(ns net.cassiel.svg.forms.l-system
  "Simple L-system division of the square."
  (:require [net.cassiel.svg.protocols :as px]
            [goog.string :as gstring]
            [goog.string.format]))

(defn l-iterate
  "Replace a randomly-selected `X` with `[XXXX]`."
  [phrase]
  )
